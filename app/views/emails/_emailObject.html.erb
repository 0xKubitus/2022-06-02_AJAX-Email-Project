 <tr id="email-<%=email.id%>-object">
  <td>
    <%= link_to "#{email.object}", email_path(email), id: "email-#{@email.id}-object-link", class: "is-unread new-email email-link", remote: true %>
  </td>
</tr>