<p id="notice"><%= notice %></p>

<h1>Mon essai de tableau à 2 colonnes : </h1>

<br>
<br>

<%= button_to 'Recevoir un email', emails_path, class: "btn btn-primary", remote: true %>

<br>
<br>

<table class="table">
  <thead class="thead-dark">
    <tr>
      <th>Liste des emails (email.object)</th>
      <th>Email sélectionné (email.body)</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @emails.each do |email| %>
      <tr>
        <td id="email-ID-<%= email.id %>"><%= link_to "#{email.object}", email_path(email.id), remote: true %></td>     

      </tr>
    <% end %>
  </tbody>
</table>

<br>
<br>
<br>
<br>
<br>
<br>

<div id="main-container">

  <div id="mail-list">
    <ul>
      <% @emails.each do |email| %>
        <li id="email-ID-<%= email.id %>"><%= link_to "#{email.object}", email_path(email.id), remote: true %></li>
      <% end %>
    </ul>

  </div>

  <div id="opened-mail">
    <p>bla</p>
  </div>

</div>



